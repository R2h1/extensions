{
  "name": "@extensions/reading-community",
  "version": "0.1.0",
  "description": "书搭子 — 浏览器随手记笔记的读书搭子",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:bundle && npm run build:copy",
    "build:bundle": "node scripts/build-bundle.cjs",
    "build:copy": "node scripts/copy-assets.cjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}